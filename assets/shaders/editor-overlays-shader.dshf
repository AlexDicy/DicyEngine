#version 410 core

layout (location = 0) out vec4 color;

in vec2 vTextureCoords;

uniform usampler2DMS uStencilTexture;

void main() {
    ivec2 coords = ivec2(gl_FragCoord.xy);
    float stencil = texelFetch(uStencilTexture, coords, 0).r;
    color = vec4(stencil, stencil, stencil, 1.0);
}
