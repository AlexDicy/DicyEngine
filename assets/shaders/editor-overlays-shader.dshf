#version 410 core

layout (location = 0) out vec4 color;

in vec2 vTextureCoords;

uniform usampler2D uDataTexture;

void main() {
    float stencil = texture(uDataTexture, vTextureCoords, 0).r;
    color = vec4(stencil);
}
